# 전체 개발/검증용 의존성(steps/* + 로컬 MCP + REST 서버 포함)
# 권장: 프로젝트 루트에서 설치
#   pip install -r requirements.txt
# SBG(skip-bigram): kiwipiepy 0.22+ 기본 모델에는 skipbigram/sj.knlm이 없으므로
# 설치 후 1회 실행 필요:
#   python scripts/install_kiwi_sbg_addon.py
pandas
numpy
openpyxl
scikit-learn
fastmcp
mcp
kiwipiepy
hdbscan
gensim
openai
sentence-transformers
google-generativeai
umap-learn
fastapi
uvicorn
# DB-ification (P0): PostgreSQL 접속/풀, 공유 .env 로딩
psycopg[binary]
psycopg_pool
python-dotenv

# === 통합(Grand Integration) 추가 의존성 ===
# Qdrant 읽기/쓰기(Stage0 적재 ingest 포함) · 임베딩 디바이스 감지
qdrant-client
torch
# Stage0 수집: BigKinds·Naver 셀레니움 수집기
selenium
webdriver-manager
requests
# Stage0/P5 Agentic 스크래퍼: Playwright + ReAct(LangGraph)
playwright
playwright-stealth
beautifulsoup4
lxml
pydantic
pydantic-settings
langgraph
langchain-core
langchain-google-genai
langchain-ollama
langchain-anthropic
# Stage7 인리치먼트: 수요(Google Trends)·소셜/경쟁(YouTube/Naver API)
pytrends
google-api-python-client
